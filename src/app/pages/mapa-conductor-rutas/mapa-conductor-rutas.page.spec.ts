/*
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MapaConductorRutasPage } from './mapa-conductor-rutas.page';

describe('MapaConductorRutasPage', () => {
  let component: MapaConductorRutasPage;
  let fixture: ComponentFixture<MapaConductorRutasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MapaConductorRutasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/